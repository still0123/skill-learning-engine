Propose one atomic edit to the named Skill. The edit must replace one exact text span,
be motivated by supplied Pattern and trace IDs, and express a reusable procedure rather
than task answers. Do not edit files directly. Call `submit_result` exactly once.
