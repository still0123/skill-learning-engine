# Purpose

Transform a JSON object containing `records` by applying its `operations` in the exact listed order.

This seed skill intentionally provides only the output and operation-order contract. It omits
detailed operation semantics, benchmark answers, and task-specific examples so the learning loop
has room to improve its procedure from execution traces.
